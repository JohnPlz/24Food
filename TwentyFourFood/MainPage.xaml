<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:strings="clr-namespace:TwentyFourFood.Resources.Strings"
             x:Class="TwentyFourFood.MainPage"
             Title="{x:Static strings:AppResources.IngredientsTitle}"
             BackgroundColor="{StaticResource BulmaBackground}">

    <CollectionView x:Name="IngredientsCollection"
                    ItemsSource="{Binding Ingredients}"
                    SelectionMode="None">
        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="12" />
        </CollectionView.ItemsLayout>

        <CollectionView.Header>
            <VerticalStackLayout Padding="24" Spacing="16">
                <Label Text="{x:Static strings:AppResources.IngredientsTitle}" Style="{StaticResource PageTitle}" />
                <Label Text="{x:Static strings:AppResources.IngredientsSubtitle}" Style="{StaticResource PageSubtitle}" />

                <Border Style="{StaticResource BulmaBox}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="{x:Static strings:AppResources.NewIngredientTitle}" Style="{StaticResource SectionTitle}" />

                        <VerticalStackLayout Spacing="6">
                            <Label Text="{x:Static strings:AppResources.NameLabel}" Style="{StaticResource FieldLabel}" />
                            <Border Style="{StaticResource BulmaInputBorder}">
                                <Entry Text="{Binding Name}"
                                       Placeholder="{x:Static strings:AppResources.NamePlaceholder}"
                                       Style="{StaticResource BulmaEntry}" />
                            </Border>
                        </VerticalStackLayout>

                        <VerticalStackLayout Spacing="6">
                            <Label Text="{x:Static strings:AppResources.CategoryLabel}" Style="{StaticResource FieldLabel}" />
                            <Border Style="{StaticResource BulmaInputBorder}">
                                <Entry Text="{Binding Category}"
                                       Placeholder="{x:Static strings:AppResources.CategoryPlaceholder}"
                                       Style="{StaticResource BulmaEntry}" />
                            </Border>
                        </VerticalStackLayout>

                        <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                            <VerticalStackLayout Grid.Column="0" Spacing="6">
                                <Label Text="{x:Static strings:AppResources.QuantityLabel}" Style="{StaticResource FieldLabel}" />
                                <Border Style="{StaticResource BulmaInputBorder}">
                                    <Entry Text="{Binding QuantityText}"
                                           Placeholder="{x:Static strings:AppResources.QuantityPlaceholder}"
                                           Keyboard="Numeric"
                                           Style="{StaticResource BulmaEntry}" />
                                </Border>
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Column="1" Spacing="6">
                                <Label Text="{x:Static strings:AppResources.UnitLabel}" Style="{StaticResource FieldLabel}" />
                                <Border Style="{StaticResource BulmaInputBorder}">
                                    <Entry Text="{Binding Unit}"
                                           Placeholder="{x:Static strings:AppResources.UnitPlaceholder}"
                                           Style="{StaticResource BulmaEntry}" />
                                </Border>
                            </VerticalStackLayout>
                        </Grid>

                        <VerticalStackLayout Spacing="6">
                            <Label Text="{x:Static strings:AppResources.NotesLabel}" Style="{StaticResource FieldLabel}" />
                            <Border Style="{StaticResource BulmaInputBorder}">
                                <Editor Text="{Binding Notes}"
                                        Placeholder="{x:Static strings:AppResources.NotesPlaceholder}"
                                        AutoSize="TextChanges"
                                        Style="{StaticResource BulmaEditor}" />
                            </Border>
                        </VerticalStackLayout>

                        <Button Text="{x:Static strings:AppResources.AddIngredientButton}"
                                Command="{Binding AddIngredientCommand}"
                                Style="{StaticResource PrimaryButton}" />

                        <Label Text="{Binding StatusMessage}" Style="{StaticResource StatusText}">
                            <Label.Triggers>
                                <DataTrigger TargetType="Label" Binding="{Binding StatusMessage}" Value="">
                                    <Setter Property="IsVisible" Value="False" />
                                </DataTrigger>
                            </Label.Triggers>
                        </Label>
                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
        </CollectionView.Header>

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Border Style="{StaticResource BulmaCard}">
                    <VerticalStackLayout Spacing="8">
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                            <Label Text="{Binding Name}" Style="{StaticResource CardTitle}" />
                                <Button Text="{x:Static strings:AppResources.DeleteButton}"
                                    Command="{Binding BindingContext.DeleteIngredientCommand, Source={x:Reference IngredientsCollection}}"
                                    CommandParameter="{Binding .}"
                                    Style="{StaticResource DeleteButton}" />
                        </Grid>

                        <HorizontalStackLayout Spacing="8">
                            <Border Style="{StaticResource TagBorder}" IsVisible="{Binding HasCategory}">
                                <Label Text="{Binding Category}" Style="{StaticResource TagText}" />
                            </Border>
                            <Border Style="{StaticResource TagBorder}" IsVisible="{Binding HasQuantity}">
                                <Label Text="{Binding QuantityDisplay}" Style="{StaticResource TagText}" />
                            </Border>
                        </HorizontalStackLayout>

                        <Label Text="{Binding Notes}" Style="{StaticResource CardNotes}" IsVisible="{Binding HasNotes}" />
                        <Label Text="{Binding CreatedAtDisplay}" Style="{StaticResource MetaText}" />
                    </VerticalStackLayout>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>

        <CollectionView.EmptyView>
            <VerticalStackLayout Padding="24" Spacing="6">
                <Label Text="{x:Static strings:AppResources.EmptyTitle}" Style="{StaticResource CardTitle}" />
                <Label Text="{x:Static strings:AppResources.EmptySubtitle}" Style="{StaticResource MetaText}" />
            </VerticalStackLayout>
        </CollectionView.EmptyView>
    </CollectionView>

</ContentPage>

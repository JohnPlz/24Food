<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TwentyFourFood.MainPage"
             Title="Ingredients"
             BackgroundColor="{StaticResource BulmaBackground}">

    <CollectionView x:Name="IngredientsCollection"
                    ItemsSource="{Binding Ingredients}"
                    SelectionMode="None">
        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="12" />
        </CollectionView.ItemsLayout>

        <CollectionView.Header>
            <VerticalStackLayout Padding="24" Spacing="16">
                <Label Text="Ingredients" Style="{StaticResource PageTitle}" />
                <Label Text="Create ingredients and store them locally with LiteDB." Style="{StaticResource PageSubtitle}" />

                <Border Style="{StaticResource BulmaBox}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="New Ingredient" Style="{StaticResource SectionTitle}" />

                        <VerticalStackLayout Spacing="6">
                            <Label Text="Name" Style="{StaticResource FieldLabel}" />
                            <Border Style="{StaticResource BulmaInputBorder}">
                                <Entry Text="{Binding Name}"
                                       Placeholder="e.g. Roma tomato"
                                       Style="{StaticResource BulmaEntry}" />
                            </Border>
                        </VerticalStackLayout>

                        <VerticalStackLayout Spacing="6">
                            <Label Text="Category" Style="{StaticResource FieldLabel}" />
                            <Border Style="{StaticResource BulmaInputBorder}">
                                <Entry Text="{Binding Category}"
                                       Placeholder="Produce, Dairy, Pantry"
                                       Style="{StaticResource BulmaEntry}" />
                            </Border>
                        </VerticalStackLayout>

                        <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                            <VerticalStackLayout Grid.Column="0" Spacing="6">
                                <Label Text="Quantity" Style="{StaticResource FieldLabel}" />
                                <Border Style="{StaticResource BulmaInputBorder}">
                                    <Entry Text="{Binding QuantityText}"
                                           Placeholder="2.5"
                                           Keyboard="Numeric"
                                           Style="{StaticResource BulmaEntry}" />
                                </Border>
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Column="1" Spacing="6">
                                <Label Text="Unit" Style="{StaticResource FieldLabel}" />
                                <Border Style="{StaticResource BulmaInputBorder}">
                                    <Entry Text="{Binding Unit}"
                                           Placeholder="kg, pcs, tsp"
                                           Style="{StaticResource BulmaEntry}" />
                                </Border>
                            </VerticalStackLayout>
                        </Grid>

                        <VerticalStackLayout Spacing="6">
                            <Label Text="Notes" Style="{StaticResource FieldLabel}" />
                            <Border Style="{StaticResource BulmaInputBorder}">
                                <Editor Text="{Binding Notes}"
                                        Placeholder="Optional details"
                                        AutoSize="TextChanges"
                                        Style="{StaticResource BulmaEditor}" />
                            </Border>
                        </VerticalStackLayout>

                        <Button Text="Add Ingredient"
                                Command="{Binding AddIngredientCommand}"
                                Style="{StaticResource PrimaryButton}" />

                        <Label Text="{Binding StatusMessage}" Style="{StaticResource StatusText}">
                            <Label.Triggers>
                                <DataTrigger TargetType="Label" Binding="{Binding StatusMessage}" Value="">
                                    <Setter Property="IsVisible" Value="False" />
                                </DataTrigger>
                            </Label.Triggers>
                        </Label>
                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
        </CollectionView.Header>

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Border Style="{StaticResource BulmaCard}">
                    <VerticalStackLayout Spacing="8">
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                            <Label Text="{Binding Name}" Style="{StaticResource CardTitle}" />
                            <Button Text="Delete"
                                    Command="{Binding BindingContext.DeleteIngredientCommand, Source={x:Reference IngredientsCollection}}"
                                    CommandParameter="{Binding .}"
                                    Style="{StaticResource DeleteButton}" />
                        </Grid>

                        <HorizontalStackLayout Spacing="8">
                            <Border Style="{StaticResource TagBorder}" IsVisible="{Binding HasCategory}">
                                <Label Text="{Binding Category}" Style="{StaticResource TagText}" />
                            </Border>
                            <Border Style="{StaticResource TagBorder}" IsVisible="{Binding HasQuantity}">
                                <Label Text="{Binding QuantityDisplay}" Style="{StaticResource TagText}" />
                            </Border>
                        </HorizontalStackLayout>

                        <Label Text="{Binding Notes}" Style="{StaticResource CardNotes}" IsVisible="{Binding HasNotes}" />
                        <Label Text="{Binding CreatedAtDisplay}" Style="{StaticResource MetaText}" />
                    </VerticalStackLayout>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>

        <CollectionView.EmptyView>
            <VerticalStackLayout Padding="24" Spacing="6">
                <Label Text="No ingredients yet." Style="{StaticResource CardTitle}" />
                <Label Text="Add your first ingredient to see it here." Style="{StaticResource MetaText}" />
            </VerticalStackLayout>
        </CollectionView.EmptyView>
    </CollectionView>

</ContentPage>
